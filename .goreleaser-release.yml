version: 2
builds:
  - skip: true
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
release:
  draft: true
  mode: append
  extra_files:
    - glob: ./go-wave-function-collapse-build/**/*.tar.gz
    - glob: ./go-wave-function-collapse-build/checksums.txt
  name_template: "Release {{.Tag}}"
  header: |
    # What's Changed
    ## 🚀 Features
    ## 🎄 Enhancements
    ## 🐛 Bug Fixes